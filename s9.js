/*9. Encontre a posição da primeira ocorrência de uma palavra dentro de uma string
usando .indexOf().*/
let frase = "sou uma bela mulher, muito bela";
frase = frase.split(" ");

console.log(frase.indexOf("bela")+1);
